CHIP A6Q2201840128 {
    IN a, b, c, d;
    OUT f;

    PARTS:
    Not(in=a, out=notA);
    Not(in=b, out=notB);
    Not(in=c, out=notC);
    Not(in=d, out=notD);

    And(a=b, b=notC, out=bnotc);
    And(a=bnotc, b=d, out=part1);

    And(a=notA, b=b, out=notab);
    And(a=c, b=notD, out=cnotd);
    And(a=notab, b=cnotd, out=part2);

    And(a=a, b=notB, out=anotb);
    And(a=anotb, b=cnotd, out=part3);

    Or(a=part1, b=part2, out=or1);
    Or(a=part3, b=or1, out=f);

}